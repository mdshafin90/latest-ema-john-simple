/**
 * FIREBASE *
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. build > Authentication > get started > enable sign in methods
 * 6. create sign up and sign in rote
 */

/**
* FOR CONTEXT
* 1. create a context provider file
* 2. create a context and set provider
* 3. set the children props
* 4. set the context value
* 5. set the provider
*/

/**
 * HOSTING
 * One Time per pc
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For Each Project one time
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow) use space bar to select
 * 4. use an existing project
 * 5.select my project name carefully
 * 6. which project as public directory: dist(vite) & build(react)
 * 7. single page application: yes
 * 8. continious deployment: no
 * 9.  
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy 
*/